node_modules
dist
.env.local
*.local
.DS_Store
npm-debug.log*
coverage
.firebase
.firebaserc
firebase-debug.log
firestore-debug.log
.angular
.claude/settings.local.json

# Generated at build time by scripts/generate-environment.mjs from
# .env.local (local) or GitHub Actions secrets (CI). Not committed so
# local Firebase project values don't leak into git history.
src/environments/environment.ts
